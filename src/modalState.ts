import { ref } from 'vue'

export const settingsModalOpen = ref(false)
export const settingsModalInitialTab = ref<string | null>(null)
export const helpModalOpen = ref(false)
export const helpModalInitialTab = ref<string | null>(null)
